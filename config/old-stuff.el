
; garak
; (setq elim-executable "~/bin/elim-client")
; (setq garak-display-splash nil)
; (setq garak-hide-offline-buddies t)
; (require 'garak)

; erc
;(setq erc-password (gnome-keyring-get-password "erc-freenode"))
;(setq erc-prompt-for-password nil)
;(setq erc-autojoin-channels-alist '(("irc.gnome.org"
;                                    "#gnome" "#evolution")
;                                   ("freenode.net"
;                                    "#gnomefr" "#pastaga" "#schurger.org"
;                                    "#fedora" "#linux-quebec" "#conkeror"
;                                     "#emacs" "#pylons" "#montreal-python"
;                                     "#ubity")))
;(setq erc-track-enable-keybindings nil)